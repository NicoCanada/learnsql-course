title: LearnSQL
description: LearnSQL v1
template:
  repo: learnsql-template
  name: learnsql
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
